import Component from "@/app/components/component";

const page = () => {
  return (
    <main className="mx-auto flex h-screen max-w-2xl items-center">
      <Component />
    </main>
  );
};

export default page;
